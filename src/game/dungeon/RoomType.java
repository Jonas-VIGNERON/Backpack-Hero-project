package game.dungeon;

/**
 * Enumeration representing the different types of rooms available in the
 * dungeon.
 */
public enum RoomType {
	EMPTY, ENEMY, TREASURE, MERCHANT, HEALER, EXIT, BLOCKED, GATE;
}